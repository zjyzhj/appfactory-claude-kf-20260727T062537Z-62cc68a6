# Value score · pm_docs attempt-1 (S3 ranking)

Gates already applied: global dedupe (store empty — no collisions), competition scans
(reject_saturated_clone: paint-mix-01, plant-light-01 — dropped, not scored).

## Value score (required for S3 ranking)

| candidate_id | market_depth | differentiation_room | v0_aha | novelty | iteration_runway | total | evidence_refs |
|--------------|--------------|----------------------|--------|---------|------------------|-------|---------------|
| home-view-01 | 4 — rental/buy search portals carry 1.4M+ ratings (Trulia 1412937), the *viewing-decision* moment behind them is mass-market; direct job unserved (nearest direct clones Home Hunter 6754038639 / Home viewing 6774289434 have 0 ratings) | 4 — portals do search not decision compare (L0 weak match); two direct clones are unproven zero-rating entrants; wedge = room-by-room photo-evidence weighted compare, offline zero-account | 4 — aha in ~30s: score 3 rooms on first visit, see weighted ranking vs a second viewing; fully local | 5 — dedupe store empty; distinct from prior package Waitwise (game_skill riichi trainer) in domain AND skeleton (decision_assistant) | 4 — criteria templates, move-in cost sheets, per-city checklists, export reports; multi-year usage cycles (each move reactivates); NOT a one-screen gadget | 21 | research/appstore-scan/home-view-01.md, dedupe store (empty), research/demand-radar/home-view-01.md (skipped_unavailable) |
| stitch-gauge-01 | 3 — knitting apps healthy (YarnPal 29992, Loopsy 13532 ratings) but core spend flows to pattern/social incumbents; gauge-math moment is real but narrower | 2 — collision high: 9 active L2 counters/calculators with 4.6–4.9 ratings; scan wedge_vs_top_n returned "insufficient evidence of wedge gap" on all Top-3; free entrenched counters compress paid room | 3 — aha = instant gauge conversion table; useful but incumbents already deliver counters well offline | 5 — dedupe store empty; distinct domain/skeleton (action_tool) from prior package | 3 — calculators + multi-project counters have runway (chart math, sleeve/raglan graders) but incumbents iterate too | 16 | research/appstore-scan/stitch-gauge-01.md, dedupe store (empty), research/demand-radar/stitch-gauge-01.md (skipped_unavailable) |

**Ranking:** 1. home-view-01 (21) — won on differentiation_room + market_depth: direct job unserved by portals, incumbents at 0 ratings. 2. stitch-gauge-01 (16) — real pain but high collision with entrenched free counters weakens wedge evidence.

**S3 selection reasoning:** select home-view-01 (total 21). Deciding axes: differentiation_room (4 vs 2 — scan Top-N leaves the wedge open) and market_depth (4 vs 3 — mass-market moment, direct competitors unproven). novelty ties (both 5, store empty). Demand-radar verbatim collection was unavailable (literal skip notes); market_depth leans on scan rating counts only, recorded as caveat.
